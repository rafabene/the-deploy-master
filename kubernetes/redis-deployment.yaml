apiVersion: apps/v1 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: redis-master
  labels:
    app: redis
    demo: the-deploy-master
spec:
  selector:
    matchLabels:
      app: redis
      demo: the-deploy-master
  replicas: 1
  template:
    metadata:
      labels:
        app: redis
        demo: the-deploy-master
    spec:
      containers:
      - name: master
        image: redis
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 6379
